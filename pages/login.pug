Document-Title 登录

.account
  SiteLayout(footer=false)
    .login
      if state.states
        .account-wrapper
          .account-wrapper-slogan 欢迎登录 #{ props.isvInfo.name }
          SimpleForm(_ref=form states=state.states onSubmit=onSubmit layout="vertical")
          #noCaptchaCode.slide-verify-code
          if state.errorMessage
            .account--error= state.errorMessage
          Button(type="primary" size="large" onClick=form.submit loading=form.isLoading() disabled=form.isLoading()) 登录
          .account--links
            Link(href=`/register${ state.redirectQuery }`): a 立即注册
            Link(href="/forget-password"): a 忘记密码
