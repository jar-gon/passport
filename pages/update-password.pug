SiteLayout(footer=false)
  if state.states
    .account-password
      .account-password-title 重置用户密码
      Alert(type="warning" message="请重置密码后访问云计算控制台。" showIcon)
      .account-password-form
        SimpleForm(_ref=form states=state.states onSubmit=onSubmit hideRequiredMark)
        if state.errorMessage
          .account-password-form-error= state.errorMessage
        Button(type="primary" size="large" onClick=form.submit loading=form.isLoading() disabled=form.isLoading()) 确认重置
