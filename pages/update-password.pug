.account
  SiteLayout(footer=false)
    if state.states
      .login
        .account-wrapper
          .account-wrapper-slogan 重置用户密码
          SimpleForm(_ref=form states=state.states onSubmit=onSubmit layout="vertical")
          if state.errorMessage
            .account--error= state.errorMessage
          Button(type="primary" size="large" onClick=form.submit loading=form.isLoading() disabled=form.isLoading()) 确认重置
